# Arduino-Virtual-Keyboard-Mouse
Convert an arduino Leonardo, into a virtual keyboard/mouse, using commands over serial.

Example python script provided that uses it. (requires pyserial to be installed).
